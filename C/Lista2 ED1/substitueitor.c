#include <stdio.h>
int main(){
    char frase[10001], palavra[41], 
         substituir[41], fraseNova[10001]=" "; 

    scanf("%[\n]", &frase);
    scanf("%s", &palavra);
    scanf("%s", &substituir);

    for (int i = 0; frase[i] != '\n'; i++) {
        
    }
    
    return 0;
}